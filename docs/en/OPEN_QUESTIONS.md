# AUB — Open questions

Product and architecture questions that are **not decided**. Assumptions are labelled. Do not treat PRELIMINARY as DECIDED.

Status values: **OPEN** | **PRELIMINARY** | **DECIDED**.

Companion: [MODULE_ROADMAP.md](MODULE_ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md), [DATA_MODEL_DRAFT.md](DATA_MODEL_DRAFT.md).

---

## Decided (already approved)

| Decision | Status |
|----------|--------|
| `AUB_admin` is the central CRM / backend core | DECIDED |
| `AUB_app` is the Flutter client | DECIDED |
| Clients talk to the core only over HTTPS API | DECIDED |
| GitHub repositories are separate (`Owiiiii1/AUB_admin`, `Owiiiii1/AUB_app`) | DECIDED |
| Flutter and backend may evolve in parallel; the API contract joins them | DECIDED |
| Parent / Student must **not** become administrative web RBAC roles merely because they need login | DECIDED (identity vs RBAC) |
| A child has at most one active primary `Class` at a time | DECIDED |
| Additional activity groups (production / rehearsal / other) are **not** a `Class` | DECIDED |
| One User has exactly one primary actor / account type (`student` / `parent` / `teacher`) | DECIDED |
| If one physical person needs two roles — two separate accounts; no multi-profile identity in the current version | DECIDED |
| `customers` is a kit leftover; academy uses `students` + `parents` + `student_parent` | DECIDED (implemented 2026-09-08) |
| Core Data Model refactor must happen **before** publishing a stable mobile API | DECIDED |
| Teacher Check-in belongs to the **work day**, not to a specific lesson/session | DECIDED |
| No running grades during the period; no per-lesson electronic gradebook | DECIDED |
| A final result belongs to Student + Class + Lesson + AcademicYear (`StudentFinalResult`) | DECIDED |
| Only teachers assigned to that class lesson may fill the shared final result | DECIDED |
| The report card includes **all** `ClassLesson` disciplines for the Class on that AcademicYear | DECIDED |
| The report-card PDF is generated by an Administrator in the admin panel; teachers do not generate PDFs | DECIDED |
| Productions / Shows are a late future / discovery-needed stage | DECIDED (priority) |

---

## Core Data Model

### Primary student class (`Class`)

- **DECIDED:** a child may belong to only one primary academic class at a time. This is a mandatory business rule of the current version.
- **Target terminology:** `Class` is the child’s permanent primary academic class. Do not mix `Class` with temporary additional groups.
- **Code today:** unique `student_id` on `academy_class_student`. PHP model `AcademyClass` / table `academy_classes`.
- **Status:** the rule is DECIDED; PHP vs product naming is fixed (`AcademyClass` = `Class`).

### Students / parents vs `customers`

- **DECIDED / implemented:** `customers` is not academy Student. Entities: `students`, `parents`, `student_parent`.
- **Production fact:** test `customers` / schedule / course_groups data was cleared by the 2026-09-08 migration. Admin users were kept.
- **Status:** implemented. Identity Layer (User binding) is **implemented** 2026-09-08.

### Unique vs additional groups

- **DECIDED:** one active `Class` **and**, at the same time, one or more additional groups (events, productions, rehearsals, etc.).
- Additional groups are **not** stored as a second `Class`.
- **Status:** product rule DECIDED. Additional-group schema must not be finalized now.

---

## Enrollment

- **Question:** Enrollment statuses for `Class`, transfer rules between classes, history, start/end dates?
- **Why it matters:** Current pivot has no workflow.
- **Current assumption:** None. The “one active Class” rule is already DECIDED; the workflow around it is not.
- **Status:** OPEN

---

## Identity & Accounts

**DECIDED for the current version:**

- One User has exactly one primary account type.
- Primary mobile actor types: `student`, `parent`, `teacher`.
- One User **cannot** be Student and Parent, Teacher and Parent, etc. at the same time.
- If one physical person needs two roles, create two separate accounts.
- Do **not** design multi-profile identity into the current version. This is a conscious MVP simplification.
- Authentication account type and administrative web RBAC are **different** concepts. Administrative staff continues to use the existing web RBAC.

**Implemented 2026-09-08:**

- `users.account_type`: `staff` | `student` | `parent` | `teacher` (string, not a DB enum).
- `users.is_active` (default true); inactive users cannot web-login.
- One User = one actor type. Cross-table links are rejected by `AccountIdentityService`.
- `staff` has no actor profile. `account_type=staff` grants no web rights; only RBAC does.
- `student` / `parent`: `role_id` is null; web admin is denied.
- `teacher`: optional web `role_id` for a workplace.
- Admin UI: Account on Student / Parent-in-student / Teacher; Settings → Users shows type/status/linked profile.
- Existing users migrated to `staff`. Teachers were **not** auto-linked by email.

**Still OPEN / technical debt:**

| Question | Why it matters | Status |
|----------|----------------|--------|
| Parent with several children — rules via `student_parent` | Cardinality, UX, consent | OPEN (target table exists; rules do not) |
| Older student with their own account — who creates it, from when | Onboarding | OPEN |
| Invitation / activation workflow | Email invite, first-time password | OPEN (admin sets a temporary password now) |
| Separate login identifier / alias / username besides unique `users.email` | One person, two accounts currently needs two emails | OPEN (do not change uniqueness now) |
| Mobile API / token auth | Flutter login | **DECIDED/implemented** — Sanctum `/api/v1`; next stage: Flutter Authentication Foundation |
| How teacher web workplace vs mobile Teacher account should look long-term | Check-in and workplace | OPEN (direction: Teacher is an actor type; web RBAC stays separate) |

---

## Roles & Access

- **Question:** Final field-level matrix (admin / secretariat / teacher / parent / student) and scoped teacher access (only assigned classes/children).
- **Why it matters:** Today any role that can open `customers.*` sees all student fields. `always_allowed_route_patterns` is wider than role menu.
- **Current assumption:** Intent matrix in [USER_ROLES_AND_ACCESS.md](USER_ROLES_AND_ACCESS.md) is **not** code.
- **Status:** OPEN

Security items below are **mandatory foundation before a wide mobile rollout** (not implemented): private storage for children’s files; field-level ACL; scoped teacher access; access/view audit of sensitive data; API authorization matrix; mobile token security; **2FA for administrative staff**; consent/privacy records.

---

## Student Attendance

Teacher write MVP is **implemented** 2026-09-09. Identity: **Student + ScheduledLesson** (`attendance_records`). Statuses: `present` / `absent` / `excused`. Unmarked = no row. **`no AttendanceRecord` ≠ `absent`**. Roster = current `academy_class_student`. Student/Parent month history is **implemented** (`GET /attendance`, `GET /children/{student}/attendance`). See [ATTENDANCE.md](ATTENDANCE.md).

This is **not** Teacher Check-in.

### Remaining OPEN

| Question | Why it matters | Status |
|----------|----------------|--------|
| Attendance finalization / edit window (lock after N days) | Accidental late edits vs operational corrections | OPEN |
| Historical roster / enrollment snapshot at lesson date | Current membership may differ from who belonged on that day | OPEN (debt; not built) |
| Student / Parent attendance history UI | Flutter month history after this API | OPEN (Flutter follow-up in `AUB_app`) |
| Extra statuses (`late`, sick, remote, comments) | Product scope | OPEN (not in MVP) |

---

## Teacher Check-in / Staff Presence

**Domain:** proof that a teacher is **physically at the academy**. Distinct from Student Attendance.

**DECIDED semantics:** check-in belongs to the **work day**, **not** to a specific lesson/session. Do not require check-in before every lesson.

**DECIDED workflow:**

1. Teacher opens the Flutter app.
2. Taps **Пришёл** / Check-in.
3. The app takes **one** geolocation snapshot (not background tracking).
4. Backend checks the academy-location geofence.
5. A **daily** teacher check-in is created.

**Conceptual data (not a final schema):** teacher; date; checked_in_at; latitude; longitude; accuracy; academy_location; status; manual correction metadata; audit.

QR is an **optional fallback**, not the primary mechanism.

### Remaining OPEN

| Question | Why it matters | Status |
|----------|----------------|--------|
| Exact geofence radius | False rejects vs cheating | OPEN |
| Minimum acceptable GPS accuracy | Indoor GPS is poor | OPEN |
| Allowed check-in time window | Prevents early/late noise | OPEN |
| Anti-spoofing | Fake location apps | OPEN |
| Extra security signals (Wi-Fi / device attestation / other) | Security vs friction | OPEN |

The module is implemented **after** Teacher identity / app foundation. See [MODULE_ROADMAP.md](MODULE_ROADMAP.md).

---

## Final Assessment / Report Cards

AUB does **not** use a regular school model of continuous grades.

**DECIDED:**

- no running grades during the academic period;
- no electronic gradebook of per-lesson scores;
- only final results are used;
- the report card is produced at the end of the academic period / academic year.

**DECIDED structure (conceptual; not a final schema / no migrations):**

```
AcademicYear → Class → ClassLesson → TeacherAssignment
Student + Class + Lesson + AcademicYear → StudentFinalResult
Student + Class + AcademicYear → ReportCard
```

- The `ClassLesson` set defines report-card disciplines. Do **not** build the discipline list by hand.
- The final result does **not** belong to a teacher. One shared `StudentFinalResult` per subject, even when several teachers are assigned.
- Only teachers assigned to that `ClassLesson` may fill / edit the final result.
- The report card includes **all** class disciplines for the AcademicYear. If any required result is missing, the administrator must get a warning before finalization / PDF generation.
- PDF is **mandatory**. An **Administrator** generates it in the admin panel. Teachers do not generate PDFs. The PDF is **not** a data source of its own.

Preliminary `ReportCard`: student_id; class_id; academic_year_id; status (`draft` / `finalized` / `printed`); finalized_at; generated_at; generated_by. Exact lifecycle may be refined later.

This is a **separate product module**, not part of API Foundation.

### Remaining OPEN

| Question | Why it matters | Status |
|----------|----------------|--------|
| Final-result scale | Schema and UI | OPEN |
| Result format | Numbers / letters / levels / text | OPEN |
| Whether `teacher_comment` is required | UX + PDF | OPEN |
| Who may finally close the report card | Workflow | OPEN |
| Whether a result may be edited after `finalized` | Audit + ops | OPEN |
| Separate approval workflow? | Official records | OPEN |
| Digital signature? | Legal/ops | OPEN |
| PDF format and official template | Print | OPEN |
| Store a PDF snapshot after printing? | Archive | OPEN |
| Regeneration / versioning rules | Ops | OPEN |
| What student vs parent may see | Privacy + Flutter | OPEN |
| Exams as a separate session type? | Calendar; **not** running grades | OPEN |
| Periods: semester / trimester / year / other | When the report card is produced | OPEN |

---

## Scheduling

Implemented today: `scheduled_lessons` (regular lessons, Mon–Fri board). **Do not change that table in a docs-only task.**

Future calendar should show at least: regular lessons; later — rehearsals / activity groups; possibly exams; possibly academy events.

**Architecture (Tech Lead has not chosen):**

- **Variant A:** one `ScheduledSession` with types (`lesson`, `rehearsal`, `exam`, `performance`, …).
- **Variant B:** separate domain entities (`ScheduledLesson`, `Rehearsal`, `Performance`, …) plus an aggregating calendar/scheduling layer.

| Question | Status |
|----------|--------|
| Variant A vs B | OPEN |
| How additional-group schedules join the **child unified calendar** | Future requirement DECIDED; implementation OPEN |

---

## Productions / Shows / additional groups

**DECIDED:**

- Productions / Shows are a **late** roadmap stage (future / discovery-needed). Do not raise priority without a separate PM decision.
- Production / rehearsal / activity groups **≠** `Class`.
- A child may belong to one primary `Class` **and** one or more additional groups.
- Those groups’ schedules must join the child’s unified calendar in the future.

**Do not do now:** a final schema; a detailed Production workflow.

Preliminary **future** types (not a final schema): `ProductionGroup`; `RehearsalGroup`; possibly other activity groups.

Costume Service may stay a **separate integrated service**. Production scheduling is not the same as costume rental.

**Status:** priority and the “≠ Class” rule are DECIDED; workflow / casting / tickets / fees remain OPEN / discovery-needed.

---

## Documents

- **Question:** Private vs public storage; document types; retention; who uploads.
- **Why it matters:** Files today sit on the **public** disk.
- **Status:** OPEN (private storage is required before wide mobile rollout — see Security)

---

## Communications

- **Question:** Channels (in-app, email, push), who may message whom, templates.
- **Status:** OPEN (`/communication` is a placeholder; student-list icon is dead)

---

## Payments

- **Question:** Italian fiscal invoices, methods, debt, link to productions/fees/tickets.
- **Status:** OPEN

---

## Privacy / Consent

Do **not** fix a specific age threshold in the domain model. Do **not** use unconfirmed wording such as “under 14” as a product rule.

**Target direction (not implemented):**

- `ConsentType`
- `ConsentDocumentVersion`
- `ConsentRecord`

Possible consent types: privacy; data processing; photo/video; marketing; special activity.

For a minor, consent is linked to a parent/guardian **where required** by academy policy and applicable law. Do **not** hardcode specific legal age rules until a legal / compliance review.

| Question | Status |
|----------|--------|
| Which consent types are mandatory | OPEN |
| Collection UX / workflow | OPEN |
| Legal / compliance review of age rules | OPEN (do not encode a threshold before review) |
| Database entities | OPEN (direction exists; implementation does not) |

Mandatory before App Store / wide mobile.

---

## Flutter / Distribution

- **Variant A:** one Store app with student / parent / teacher **modes**.
- **Variant B:** several Store apps sharing a Flutter codebase / flavors.

Do not confuse this with identity: one User = one actor type is **DECIDED**. Store packaging (one app vs flavors) remains **OPEN**.

- **Status:** OPEN (distribution only)

---

## Infrastructure / Deployment

- **Question:** Git deploy GitHub → `/var/www/aub`; staging; backups; monitoring; CI/CD.
- **Current fact:** production is **not** a git repository.
- **Status:** OPEN
